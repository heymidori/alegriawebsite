# Public — safe to expose in the frontend bundle
VITE_TURNSTILE_SITE_KEY=

# Private — set these in Vercel Project Settings → Environment Variables, never commit real values
TURNSTILE_SECRET_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=contact@finconecta.com
